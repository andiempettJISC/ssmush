apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssmush.fullname" . }}
  labels:
    {{- include "ssmush.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssmush.selectorLabels" . | nindent 4 }}
